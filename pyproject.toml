[project]
name = "wyrdfold"
version = "0.0.0"
description = "Workspace root — not a publishable package"
license = { text = "FSL-1.1-ALv2" }
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["apps/wyrdfold-api"]
